import { Router } from 'express'; const r=Router(); r.get('/nodes',(_,res)=>res.json([])); r.post('/seed',(_,res)=>res.json({ok:true})); r.post('/layout',(_,res)=>res.json({ok:true})); r.post('/export',(_,res)=>res.json({ok:true,url:null})); export default r;
